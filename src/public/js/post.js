"use strict";
/**
 * Lets the user interact with the post.
 */
// Like
fn["post.l"] = () => {
    TODO("Like post");
};

// Show replies
fn["post.sr"] = () => {
    TODO("Show replies");
};

// Save
fn["post.s"] = () => {
    TODO("Save");
};

// Reply
fn["post.r"] = () => {
    TODO("Reply");
};

// Go to user/community
fn["post.n"] = () => {
    TODO("Go to user/community");
};

// Show timestamp/date
fn["post.t"] = () => {
    TODO("Like post");
};

fn[F]();
