"use strict";
/**
 * Lets the user interact with the post.
 */
// Like
fn[f + ".l"] = () => {
    TODO("Like post");
};

// Show replies
fn[f + ".sr"] = () => {
    TODO("Show replies");
};

// Save
fn[f + ".s"] = () => {
    TODO("Save");
};

// Reply
fn[f + ".r"] = () => {
    TODO("Reply");
};

// Go to user/community
fn[f + ".n"] = () => {
    TODO("Go to user/community");
};

// Show timestamp/date
fn[f + ".t"] = () => {
    TODO("Like post");
};

fn[F]();
