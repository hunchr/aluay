"use strict";
/**
 * Lets the user interact with the post media.
 */
// ----- Video -----
// Play/Pause
fn[f + ".p"] = () => {
    TODO("Play/Pause");
};

// Replay
fn[f + ".rp"] = () => {
    TODO("Replay");
};

// Forward
fn[f + ".fw"] = () => {
    TODO("Forward");
};

// Volume
fn[f + ".v"] = () => {
    TODO("Volume");
};

// Speed
fn[f + ".s"] = () => {
    TODO("Speed");
};

// Fullscreen
fn[f + ".f"] = () => {
    self.parentNode.parentNode.classList.toggle("fullscreen");
};

// ----- Image -----
// Previous image
fn[f + ".pi"] = () => {
    TODO("Previous image");
};

// Next image
fn[f + ".ni"] = () => {
    TODO("Next image");
};

fn[uri]();
